// Array Methods:
    // MRF -> (Map, Reduce, Filter)

// let fruits = ["apple", "banana", "orange", "grapes", "watermelon"]

let nums = [2, 4, 7, 5, 19, 17, 15, 22, 34]

// Map:

    // fruits.map((item) => console.log(item))

    // let result = nums.map((item) => item * 2);

    // console.log(result);

// Filter:

    // let result = nums.filter((item) => item % 2 != 0)

    // console.log(result);

// reduce:

    // let result = nums.reduce((acc, curr) => acc + curr)     

    // console.log(result);
